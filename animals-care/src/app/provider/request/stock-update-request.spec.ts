import { StockUpdateRequest } from './stock-update-request';

describe('StockUpdateRequest', () => {
  it('should create an instance', () => {
    expect(new StockUpdateRequest()).toBeTruthy();
  });
});
